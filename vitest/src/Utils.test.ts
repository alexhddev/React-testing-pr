import { describe, it, expect } from 'vitest'

describe('utils test suite', ()=>{
    it('check if 1 = 1', ()=>{
        expect(1).toEqual(1)
    })

    it('check if 2 = 2', ()=>{
        expect(2).toEqual(2)
    })
})